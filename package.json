{
  "name": "feishu-max-saver-skill",
  "version": "0.1.0",
  "description": "Feishu AI Skill — 4% context cost, 90% Feishu coverage, works with any Agent",
  "type": "module",
  "bin": {
    "feishu": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "commander": "^13.0.0",
    "marked": "^17.0.4",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
